global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "chrono-api"
    static_configs:
      - targets: ["chrono-api:%{ADMIN_PORT}"]
  - job_name: 'victoria-metrics'
    static_configs:
      - targets: ['victoria-metrics:8428']
